server:
  port: 8003
spring:
  application:
    name: edu-order-boot
eureka:
  client:
    service-url:
      defaultZone: http://192.168.200.14:7001/eureka
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
    instance-id: ${spring.cloud.client.ip-address}:${server.port}